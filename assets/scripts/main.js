const cardContainers = document.querySelectorAll('.cards, .team-grid, .partners-grid'); cardContainers.forEach(container => { const cards = container.querySelectorAll('.card, .team-card, .partner-card'); cards.forEach((card, i) => { card.style.opacity = '0'; card.style.transform = 'translateY(24px)'; card.style.transition = `opacity 0.5s ease ${i * 0.1}s, transform 0.5s ease ${i * 0.1}s`; }); const cardObserver = new IntersectionObserver(entries => { entries.forEach(entry => { if (entry.isIntersecting) { const cards = entry.target.querySelectorAll('.card, .team-card, .partner-card'); cards.forEach(card => { card.style.opacity = '1'; card.style.transform = 'translateY(0)'; }); cardObserver.unobserve(entry.target); } }); }, { threshold: 0.1 }); cardObserver.observe(container); }); const revealEls = document.querySelectorAll( '.hero, .stats, .section, .contact-section' ); revealEls.forEach(el => el.classList.add('reveal')); const observer = new IntersectionObserver( entries => { entries.forEach(entry => { if (entry.isIntersecting) { entry.target.classList.add('visible'); observer.unobserve(entry.target); } }); }, { threshold: 0.1 } ); revealEls.forEach(el => observer.observe(el)); const sections = document.querySelectorAll('section[id], div[id]'); const navLinks = document.querySelectorAll('.nav-links a'); const sectionObserver = new IntersectionObserver( entries => { entries.forEach(entry => { if (entry.isIntersecting) { navLinks.forEach(link => { link.style.color = link.getAttribute('href') === '#' + entry.target.id ? 'var(--gray-900)' : ''; }); } }); }, { rootMargin: '-40% 0px -55% 0px' } ); sections.forEach(s => sectionObserver.observe(s)); document.querySelectorAll('.pub-tab').forEach(tab => { tab.addEventListener('click', () => { const key = tab.dataset.tab; document.querySelectorAll('.pub-tab').forEach(t => { t.classList.toggle('active', t === tab); t.setAttribute('aria-selected', t === tab); }); document.querySelectorAll('.pub-panel').forEach(p => { p.classList.toggle('active', p.dataset.tab === key); }); }); }); const contactBtn = document.getElementById('contact-btn'); const emailInput = document.getElementById('email-input'); const feedback = document.getElementById('form-feedback'); function isValidEmail(email) { return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email); } contactBtn.addEventListener('click', () => { const email = emailInput.value.trim(); if (!email) { feedback.style.color = '#E24B4A'; feedback.textContent = 'Please enter your email address.'; emailInput.focus(); return; } if (!isValidEmail(email)) { feedback.style.color = '#E24B4A'; feedback.textContent = 'Please enter a valid email address.'; emailInput.focus(); return; } contactBtn.disabled = true; contactBtn.textContent = 'Sending…'; setTimeout(() => { feedback.style.color = '#1a56a0'; feedback.textContent = "Thanks! We'll be in touch within 48 hours."; emailInput.value = ''; contactBtn.textContent = 'Sent ✓'; }, 800); }); emailInput.addEventListener('keydown', e => { if (e.key === 'Enter') contactBtn.click(); }); emailInput.addEventListener('input', () => { feedback.textContent = ''; contactBtn.disabled = false; contactBtn.textContent = 'Get in touch →'; });